enum Classification { breeding, nonBreeding }

enum RiskLevel { red, yellow, green }

enum SyncStatus { pendingUpload, uploading, synced, failed }

enum PointsStatus { provisional, verified }

enum GroundTruth { unlabeled, breeding, nonBreeding }
